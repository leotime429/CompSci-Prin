"""
This program asks a user for some basic information and then greet them by
the name given. It then prints a phrase about the sport entered.
"""
def greeting(user_info):
    user_info_list = user_info.split()
    return "Hello, " + user_info_list[0] + "! I also enjoy " + user_info_list[2] + "!"
